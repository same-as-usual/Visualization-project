# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Node (dashboard)
node_modules/
dist/

# Env
.env

# NOTE: data/ is intentionally NOT ignored — the collect workflow commits
# raw JSONL there daily (append-only raw zone).

# Workflow files are managed via the GitHub web UI only: pushing them from a
# clone fails with "refusing to allow an OAuth App to create or update
# workflow ... without workflow scope". Edit them at github.com instead.
.github/workflows/
